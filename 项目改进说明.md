# SpaceFL 项目改进说明

> 生成日期：2026-08-11
> 基于对仓库的完整审查（代码结构、CODING_STANDARDS.md 合规性、Git 历史、外部论文对比）

---

## 目录

1. [概览](#1-概览)
2. [仓库结构清理](#2-仓库结构清理)
3. [CODING_STANDARDS.md 合规性修复](#3-coding_standardsmd-合规性修复)
4. [代码质量统一](#4-代码质量统一)
5. [Git 历史清理](#5-git-历史清理)
6. [3D 可视化退回问题](#6-3d-可视化退回问题)
7. [架构与设计改进](#7-架构与设计改进)
8. [CI/CD 与自动化](#8-cicd-与自动化)
9. [文档完善](#9-文档完善)
10. [执行优先级与排期](#10-执行优先级与排期)

---

## 1. 概览

SpaceFL 是一个架构设计优秀、选题有创新性的太空联邦学习研究框架。代码核心(`fl_space/`)质量高，但仓库管理和规范合规性严重落后于代码质量本身。本文档汇总所有已发现的问题，提供逐一修复方案。

---

## 2. 仓库结构清理

### 2.1 问题：根目录散落严重

当前根目录包含 40+ 个文件/目录，其中大量不属于仓库：

```
## 临时脚本（应移至 tests/ 或 scripts/）
_quick_fl.py          ## → test_docs/ 或 scripts/
_run_demo.py          ## → examples/
_smoke_test.py        ## → tests/
test_cli_params.py    ## → tests/
control_panel.py      ## → scripts/
control_panel.bat     ## → scripts/ 或删除

## 实验输出目录（不应提交）
fedleo_local_validation_20260810/
fedleo_local_validation_20260810_seed2/
fedleo_local_validation_20260810_seed3/
fedleo_local_validation_smoke/
fedleo_result_1/
fedleo_test_output/
fedleo_web_validation_20260811_215125_20260810/  ## 磁盘上存在，未追踪

## 未归类 Markdown 文档
验证报告.md                ## → docs/
验证报告-简版.md            ## → docs/
平台搭建完成总结.md          ## → docs/
平台部署报告.md             ## → docs/
平台功能演示说明.md          ## → docs/
终端可用性验证总结.md        ## → docs/
快速开始.md                 ## → docs/
CLI参数测试报告.md           ## → docs/
```

### 2.2 修复方案

```bash
# （1）移动临时脚本
Move-Item -LiteralPath "_quick_fl.py" -Destination "test_docs/"
Move-Item -LiteralPath "_run_demo.py" -Destination "examples/"
Move-Item -LiteralPath "_smoke_test.py" -Destination "tests/"
Move-Item -LiteralPath "test_cli_params.py" -Destination "tests/"
Move-Item -LiteralPath "control_panel.py" -Destination "scripts/"
Move-Item -LiteralPath "control_panel.bat" -Destination "scripts/"

# （2）移动未归类文档到 docs/
Move-Item -LiteralPath "验证报告.md" -Destination "docs/"
Move-Item -LiteralPath "验证报告-简版.md" -Destination "docs/"
Move-Item -LiteralPath "平台搭建完成总结.md" -Destination "docs/"
Move-Item -LiteralPath "平台部署报告.md" -Destination "docs/"
Move-Item -LiteralPath "平台功能演示说明.md" -Destination "docs/"
Move-Item -LiteralPath "终端可用性验证总结.md" -Destination "docs/"
Move-Item -LiteralPath "快速开始.md" -Destination "docs/"
Move-Item -LiteralPath "CLI参数测试报告.md" -Destination "docs/"

# （3）添加实验输出目录到 .gitignore
Add-Content -LiteralPath ".gitignore" -Value @"
## 实验输出目录（禁止提交）
fedleo_local_validation_*/
fedleo_result_*/
fedleo_test_output/
fedleo_web_validation_*/
"@

# （4）从 git 中移除已追踪的实验输出目录
git rm -r --cached fedleo_local_validation_20260810/
git rm -r --cached fedleo_local_validation_20260810_seed2/
git rm -r --cached fedleo_local_validation_20260810_seed3/
git rm -r --cached fedleo_local_validation_smoke/
git rm -r --cached fedleo_result_1/
git rm -r --cached fedleo_test_output/
```

### 2.3 修复后的根目录目标形态

```
fl_space/
├── fl_space/              ## 核心代码
├── tests/                 ## 测试
├── examples/              ## 示例脚本
├── scripts/               ## 工具脚本、控制面板
├── docs/                  ## 所有文档
├── web/                   ## Web 前端
├── README.md
├── pyproject.toml
├── .gitignore
└── ...
```

---

## 3. CODING_STANDARDS.md 合规性修复

CODING_STANDARDS.md（772行）是质量极高的规范文档，但实际代码在多个核心规则上存在系统性违反。

### 3.1 类型注解：`Optional[X]` → `X | None`

**违规规模**：75 处 `Optional[...]`，10 个文件导入 `from typing import Optional`

**标准原文**（第 3.1 节）：
```
❌ 错误 — Optional
def get_value(key: Optional[str]) -> Optional[float]:
✅ 正确 — | 联合类型
def get_value(key: str | None) -> float | None:
```

**典型修复示例**：

```python
# ❌ 修复前（orbit_simulator.py:85）
from typing import Optional, Union
body: Optional[CelestialBody] = None,

# ✅ 修复后
body: CelestialBody | None = None,

# ❌ 修复前（orbit/visibility.py:186）
) -> list[Optional[int]]:

# ✅ 修复后
) -> list[int | None]:
```

**修复策略**：逐个文件批量替换，使用 Ruff 的 `UP007` 规则自动修复：

```bash
ruff check --select UP007 --fix fl_space/
```

### 3.2 `__init__.py` 缺少 `__all__`

**违规文件**（3个）：

| 文件 | 问题 |
|------|------|
| `fl_space/__init__.py` | 无 `__all__`（顶级包，仅含 `__version__`） |
| `fl_space/config/__init__.py` | 无 `__all__`，缺少重导出 |
| `fl_space/integrations/__init__.py` | 无 `__all__`，缺少重导出 |

**修复**：

```python
# fl_space/config/__init__.py — 添加 __all__ 和重导出
from .defaults import BODY_PRESETS as BODY_PRESETS
from .defaults import CONSTELLATION_PRESETS as CONSTELLATION_PRESETS
from .defaults import EXPERIMENT_CONFIGS as EXPERIMENT_CONFIGS
from .loader import ConfigLoader as ConfigLoader
from .loader import load_config as load_config

__all__ = [
    "BODY_PRESETS",
    "CONSTELLATION_PRESETS",
    "EXPERIMENT_CONFIGS",
    "ConfigLoader",
    "load_config",
]
```

### 3.3 dataclass 缺少 `to_dict()` / `from_dict()`

**违规规模**：16 个 `@dataclass` 文件中 11 个缺失：

| 文件 | 缺失 |
|------|------|
| `fedleo/scheduler.py` | 两者都缺 |
| `fedleo/experiment.py` | 两者都缺 |
| `fedleo/planner.py` | 两者都缺 |
| `fl/core.py` | 两者都缺 |
| `fl/scheduler.py` | 两者都缺 |
| `environment/atmosphere.py` | 两者都缺 |
| `orbit/satellite_phases.py` | 两者都缺 |
| `integrations/flower/adapter.py` | 两者都缺 |
| `fedleo/metrics.py` | 缺 `from_dict` |
| `fl/time_model.py` | 缺 `from_dict` |
| `orbit/kepler_orbit.py` | 缺 `from_dict` |

**修复**：为每个 dataclass 补充 `to_dict()` 方法。如果 dataclass 是纯运行时对象不需要序列化，则标注为内部 dataclass 并注明豁免。

```python
@dataclass
class FedLEOConfig:
    """FedLEO 实验配置。"""
    # ... fields ...

    def to_dict(self) -> dict:
        """序列化为字典。"""
        return {
            "num_satellites": self.num_satellites,
            "num_planes": self.num_planes,
            "sats_per_plane": self.sats_per_plane,
            "num_rounds": self.num_rounds,
            "local_epochs": self.local_epochs,
            "batch_size": self.batch_size,
            "learning_rate": self.learning_rate,
            "device": self.device,
            "enable_offloading": self.enable_offloading,
            "seed": self.seed,
        }

    @classmethod
    def from_dict(cls, data: dict) -> "FedLEOConfig":
        """从字典反序列化。"""
        return cls(**data)
```

### 3.4 `test_cli_params.py` 使用旧式 typing 泛型

```python
# ❌ 修复前
from typing import List, Tuple

# ✅ 修复后
## 无额外导入，直接使用 list 和 tuple
```

---

## 4. 代码质量统一

### 4.1 examples/standard_experiment.py Ruff 错误

当前 `ruff_chk.txt` 显示 26 个错误：

| 规则 | 数量 | 说明 |
|------|------|------|
| E702 | 16 | 分号多语句（如 `ax.set_xlabel("R"); ax.set_ylabel("Y")`） |
| C408 | 5 | `dict()` 应写为字面量 `{}` |
| C401 | 4 | 生成器应改为集合推导式 |
| F841 | 1 | 未使用变量 `results` |

**修复**：

```bash
# 自动修复（安全的部分）
ruff check --fix examples/standard_experiment.py

# 手动修复 E702（分号拆为多行）
# 将 ax.set_xlabel("R"); ax.set_ylabel("Y") 改为：
# ax.set_xlabel("R")
# ax.set_ylabel("Y")
```

### 4.2 ruff_chk.txt 不应提交

```bash
git rm ruff_chk.txt
echo "ruff_chk.txt" >> .gitignore
```

---

## 5. Git 历史清理

### 5.1 二进制文件移除

**已追踪的二进制文件**：

| 文件 | 大小 | 处理方式 |
|------|------|----------|
| `de421.bsp` | 168 MB | `git rm --cached`，改为 Skyfield 自动下载 |
| `data/MNIST/raw/*.gz` | ~50 MB | `git rm --cached`，`.gitignore` 已有 `data/` |
| `examples/output/*.png` | 若干 | `git rm --cached` |

**修复**：

```bash
git rm --cached de421.bsp
git rm --cached -r data/MNIST/
git rm --cached -r examples/output/
## .gitignore 已有相关规则，无需再改
```

### 5.2 构建产物与缓存移除

```bash
## 移除所有被追踪的 __pycache__/
$pycache_files = git ls-files -- '*__pycache__/*'
foreach ($f in $pycache_files) { git rm --cached $f }

## 移除 fl_space.egg-info/
git rm --cached -r fl_space.egg-info/
```

### 5.3 历史 squash 建议（可选）

当前 14 次提交中多次是 "更新代码" 和功能追加。如果追求干净的历史，可以考虑：

```bash
# 交互式 rebase，将修复类提交合并
git rebase -i --root
```

**注意**：在 `git rebase` 前确认远程仓库没有其他人基于当前分支工作。

---

## 6. 3D 可视化退回问题

### 6.1 问题概述

Git 历史分析显示：commit `af11bd9`（2026-06-05）中的 `web/index.html` 原本包含完整 CesiumJS 1.120 3D 地球可视化实现。但在最新的 Web 大改造提交 `58b3c3a`（2026-08-11）中，**整个前端被重写，CesiumJS 3D 地球被 Canvas 2D 平面地图完全替代**。

| 对比维度 | 原始实现 (`af11bd9`) | 当前实现 (`58b3c3a`) |
|----------|---------------------|----------------------|
| 渲染引擎 | CesiumJS 1.120（3D 地球） | Canvas 2D（平面地图） |
| 地图底图 | Cesium Ion 3D globe + 真实影像 | 手绘经纬线 + 硬编码海岸线坐标 |
| 卫星显示 | Cesium Entity (3D 位置) | `ctx.arc()` 2D 圆点 |
| 链路显示 | Cesium Polyline | `ctx.lineTo()` 画线 |
| 地形/影像 | 真实地球影像（Ion Token） | 纯黑背景 `#07090b` |
| 视角操作 | 3D 旋转/缩放/倾斜 | 无（静态平面） |
| 播放动画 | Cesium Clock 时间推进 | `setInterval` 更新 Canvas 重绘 |

### 6.2 当前仍引用已不存在功能的文件

文档和代码在 CesiumJS 被移除后**未同步更新**，仍在宣称"3D CesiumJS 可视化"：

| 文件 | 行号 | 引用内容 |
|------|------|---------|
| `fl_space/cli.py` | 1256 | `"""启动 CesiumJS 3D 可视化服务器。"""` |
| `fl_space/cli.py` | 1288 | `print("  SpaceFL 3D 可视化服务器")` |
| `fl_space/cli.py` | 1415 | `fls run serve  启动 CesiumJS 3D 可视化` |
| `fl_space/cli.py` | 1800 | `help="CesiumJS 3D可视化"` |
| `control_panel.py` | 213 | `"启动3D轨道服务器（浏览器打开）"` |
| `平台部署报告.md` | 31 | `3D 卫星轨道可视化（Cesium）` |
| `平台部署报告.md` | 247 | `**CesiumJS** - 3D 地球引擎` |
| `设计⽅案.md` | 70 | `**CesiumJS 1.120**（现有）+ **Ion Token 动态配置**` |
| `设计⽅案.md` | 218 | `3D 视图保留现有「时隙滑块 + 播放/暂停 + 加载」控件` |
| `.codebuddy/memory/` | 多处 | `CesiumJS 3D 可视化 (web/) — CesiumJS 3D 地球 + 卫星轨迹` |

### 6.3 设计文档对照表

`docs/SpaceFL可视化智能实验平台设计方案.md` 要求但当前**不存在**的功能：

| 设计要求 | 实际状态 |
|---------|---------|
| Cesium Ion 影像底图（World Imagery） | 不存在 |
| Cesium Ion 地形（World Terrain） | 不存在 |
| Cesium Ion Token 动态配置 | 不存在 |
| 多地图源切换 | 不存在 |
| 轨道面着色 | 不存在 |
| 点击卫星查看状态卡片 | 不存在 |
| 卫星轨迹线 | 不存在 |
| `/api/settings` 返回 `cesium_ion_token` | 不存在 |

### 6.4 修复方案

**方案 A：恢复 CesiumJS 3D（推荐，回归原始设计）**

```bash
# 查看原始代码
git show af11bd9:web/index.html
git show af11bd9:web/assets/app.js

# 恢复步骤：
# 1. 申请 Cesium Ion Token: https://ion.cesium.com
# 2. 将原始 CesiumJS index.html 恢复为"轨道视图"Tab 的内容
# 3. server.py 中添加 /api/settings 返回 cesium_ion_token
# 4. 环境变量 CESIUM_ION_TOKEN 注入
# 5. /api/orbit_data 接口数据格式已适配 CesiumJS
```

**方案 B：升级到 Three.js（中等体积，可控依赖）**

如果不想引入 CesiumJS 的 Ion Token 依赖，用 Three.js 渲染带地球纹理的球体 + 卫星轨道线，体验介于 CesiumJS 和 Canvas 2D 之间。需要额外引入 Three.js 和地球纹理（~10MB）。

**方案 C：承认现状，清洗所有虚假引用**

如果决定保持 Canvas 2D，则必须：
- 修正 `cli.py` 中所有 "CesiumJS 3D" 描述为 "轨道可视化"
- 更新 `平台部署报告.md` 和所有文档
- 删除 `.codebuddy/memory/` 中的过时描述
- 修正设计方案文档状态为"待实现"

### 6.5 建议

**无论选 A/B/C，都必须清理所有虚假引用。** 当前状态——代码是 Canvas 2D 但 CLI 和文档说 CesiumJS 3D——是最大的自相矛盾。

---

## 7. 架构与设计改进

### 7.1 数据路径重复

`fedleo/experiment.py::_prepare_data()` 和 `fl/runner.py::prepare_data()` 各自实现了数据加载 + non-IID 分配逻辑，存在显著的重叠。

两者的差异：
- `experiment.py` 是独立的、不依赖 FLRunner 的工具函数
- `runner.py` 是 FLRunner 的方法，与 FL 实验生命周期绑定

**建议**：将共享的数据划分逻辑统一到 `fl/runner.py` 或新建 `fl/data_utils.py`，让 `experiment.py` 调用：

```python
# fl/data_utils.py
def prepare_data(
    dataset_name: str,
    num_clients: int,
    classes_per_client: int,
    partition_strategy: str,
    ...
) -> tuple[dict[int, DataLoader], DataLoader, list[int]]:
    """统一的数据准备入口。"""
    ...
```

### 7.2 参数膨胀问题

`FLRunner.run()` 有 19 个参数，`FLRunner.prepare_data()` 有 16 个参数。建议引入配置 dataclass：

```python
@dataclass
class DataPartitionConfig:
    """数据划分配置。"""
    dataset_name: str = "mnist"
    iid: bool = False
    alpha: float = 0.5
    classes_per_client: int = 2
    max_samples_per_client: int = 1000
    data_dir: str = "./data"
    partition_strategy: str = "probability"
    class_probability: float = 0.8
    preference_mode: str = "class_balanced"
    preferred_clients_per_class: int = 1
    sample_cap_strategy: str = "preserve"

    def to_dict(self) -> dict: ...
    @classmethod
    def from_dict(cls, data: dict) -> "DataPartitionConfig": ...
```

### 7.3 文档硬编码路径

`快速开始.md` 中包含 `cd D:/Desktop/fl_space`，应该替换为通用路径：

```markdown
# ❌ 修复前
cd D:/Desktop/fl_space

# ✅ 修复后
cd /path/to/fl_space
```

---

## 8. CI/CD 与自动化

### 8.1 创建 GitHub Actions 工作流

新建 `.github/workflows/ci.yml`：

```yaml
name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v2
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: uv pip install -e ".[full]"

      - name: Ruff lint
        run: ruff check fl_space/ tests/

      - name: Ruff format check
        run: ruff format --check fl_space/ tests/

      - name: Run tests
        run: pytest tests/ -v --timeout=300
```

### 8.2 添加 pre-commit 配置

新建 `.pre-commit-config.yaml`：

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
```

```bash
pip install pre-commit
pre-commit install
```

---

## 9. 文档完善

### 9.1 README 补充

当前 README 缺少以下内容：
- **标准实验结果表**：在统一条件下 FedAvg vs FedProx vs FedBuff vs FedLEO 的对比数据（准确率、收敛轮次、虚拟时间）
- **与 arXiv:2511.14889 的对照表**：哪些实验复现了原论文的 768 组配置，哪些是新增的
- **版本兼容性表**：Python / PyTorch / torchvision 的明确版本矩阵

### 9.2 新增文档

| 文档 | 位置 | 内容 |
|------|------|------|
| `CONTRIBUTING.md` | 根目录 | 从 CODING_STANDARDS.md 提取 PR 规范部分 |
| `CHANGELOG.md` | 根目录 | 基于 git log 生成版本变更记录 |
| `docs/实验结果.md` | docs/ | 标准条件下的实验对照表 |

---

## 10. 执行优先级与排期

### P0（立即执行，合计约 3h）

| 序号 | 任务 | 预估时间 |
|------|------|----------|
| 1 | 移动临时脚本和文档到正确目录 | 15 min |
| 2 | 添加实验输出目录到 `.gitignore` + `git rm --cached` | 10 min |
| 3 | `git rm --cached` 移除 de421.bsp、MNIST、pyc、egg-info | 10 min |
| 4 | `git rm ruff_chk.txt` 并加入 `.gitignore` | 1 min |
| 5 | 修复 `examples/standard_experiment.py` 26 个 Ruff 错误 | 20 min |
| 6 | 修复 `test_cli_params.py` 的旧式 typing 导入 | 2 min |
| 7 | 修复 `快速开始.md` 硬编码路径 | 1 min |
| 8 | 修正 `cli.py` / `control_panel.py` / 文档中所有 "CesiumJS 3D" 虚假引用 | 30 min |

### P1（本周内，合计约 10h）

| 序号 | 任务 | 预估时间 |
|------|------|----------|
| 9 | 用 Ruff UP007 修复 `Optional[X]` → `X | None` | 30 min |
| 10 | 补全 3 个 `__init__.py` 的 `__all__` 和重导出 | 20 min |
| 11 | 补全 11 个 dataclass 的 `to_dict()`/`from_dict()` | 2 h |
| 12 | 添加 `.github/workflows/ci.yml` | 30 min |
| 13 | 添加 `.pre-commit-config.yaml` | 10 min |
| 14 | 引入 `DataPartitionConfig` 重构参数膨胀 | 2 h |
| 15 | 恢复 CesiumJS 3D 可视化或实现 Three.js 替代方案 | 4 h |

### P2（未来两周）

| 序号 | 任务 | 预估时间 |
|------|------|----------|
| 14 | 统一 `experiment.py` 和 `runner.py` 的数据路径 | 3 h |
| 15 | 补充 README 中的标准实验结果表 | 1 h |
| 16 | 添加论文对照表（复现范围 vs 新增功能） | 1 h |
| 17 | 评估是否需要 Git 历史 squash | 1 h |

---

## 附录 A：违规统计汇总

| 维度 | 违规数量 | 严重程度 |
|------|----------|----------|
| `Optional[X]` 替代 `X | None` | 75 处 | 高 |
| `__all__` 缺失 | 3 个文件 | 中 |
| dataclass 缺 `to_dict()/from_dict()` | 11 个文件 | 中 |
| `from __future__ import annotations` 缺失 | 24 个文件 | 低 |
| Ruff 错误 | 26 个（examples/） | 中 |
| 二进制文件被追踪 | 4 类（bsp/gz/png/pyc） | 高 |
| 实验输出目录被提交 | 6 个目录 | 中 |
| 临时文件被提交 | ruff_chk.txt | 低 |
| 文档硬编码路径 | 1 处 | 低 |
| CI/CD 缺失 | 0 个工作流 | 中 |
| CesiumJS 3D 被移除但未同步更新引用 | 10+ 处虚假引用 | 高 |

## 附录 B：修复后验证命令

```bash
# 确认二进制文件不再被追踪
git ls-files -- '*.bsp' '*.pyc' '*.pkl' '*.gz' '*.png' | Should-BeEmpty

# 确认 Ruff 零错误
ruff check fl_space/ examples/ tests/ scripts/

# 确认 Ruff 格式化一致
ruff format --check fl_space/ examples/ tests/ scripts/

# 确认 import 正常
python -c "import fl_space; print('OK')"

# 运行全部测试
pytest tests/ -v
```
