# 2D 轨道、文献管理与 CC Switch 集成完成报告

## 完成时间

2026-08-15

## 1. 2D 轨道地图

访问地址：<http://127.0.0.1:8700/orbit_2d_viewer.html>

### 地图方案

- 地理数据：[topojson/world-atlas](https://github.com/topojson/world-atlas)
- 原始地理来源：Natural Earth
- 地图精度：110m 世界国家边界
- 投影：D3 `geoNaturalEarth1`
- 地图、D3 和 TopoJSON 均通过本地 `node_modules` 提供，不依赖运行时 CDN

`world-atlas` 使用 ISC License；Natural Earth 数据属于公共领域。

### 已实现功能

- 世界陆地、国界、海洋和经纬网渲染
- 卫星二维经纬度位置与编号显示
- 地面站位置显示
- GSL 地面链路显示
- ISL 星间链路显示开关
- 卫星完整轨迹显示开关
- 卫星数、地面站数、仿真时长和时间槽参数
- 播放、暂停和时间槽拖动
- 桌面与窄屏响应式布局
- 地图和轨道数据加载状态反馈

### 本地资源

```text
/vendor/d3/d3.min.js
/vendor/topojson/topojson-client.min.js
/vendor/world-atlas/countries-110m.json
```

## 2. 文献管理

入口：完整操作台 -> 文献库

### 已实现功能

- 浏览项目文档和默认文献目录
- 设置默认文献读取目录
- 从浏览器导入本地文献
- 支持多文件导入
- 同名文件自动生成不冲突的文件名
- 单文件大小限制为 30 MB
- 支持 PDF、Markdown、TXT
- PDF 使用浏览器内嵌预览
- Markdown 使用 `marked` 解析
- Markdown HTML 使用 `DOMPurify` 清理后再显示
- 文献路径通过目录编号和相对路径访问，阻止 `..` 路径越界

### API

```text
GET  /api/library
GET  /api/library/settings
PUT  /api/library/settings
POST /api/library/import?filename=...
GET  /api/library/content?path=...
GET  /api/library/file?path=...
```

本地目录配置保存到 `.literature_config.json`，该文件已加入 `.gitignore`。

## 3. CC Switch AI 设置

入口：完整操作台 -> AI 解读 -> 导入设置

### 数据来源

使用 CC Switch 官方项目：

- GitHub：<https://github.com/farion1231/cc-switch>
- 数据库：`~/.cc-switch/cc-switch.db`
- 表：`providers`
- 应用类型：`claude`
- 读取条件：当前供应商 `is_current = 1`

### 安全设计

- CC Switch 数据库以 SQLite 只读模式打开
- API Token 不写入 SpaceFL 项目
- API Token 不返回给网页
- SpaceFL 只保存 `source=cc_switch` 和导入时的供应商 ID
- 每次 AI 请求重新读取 CC Switch 当前 Claude 供应商
- 在 CC Switch 中切换供应商后，SpaceFL 无需复制新密钥

本地引用配置保存到 `.spacefl_ai_config.json`，该文件已加入 `.gitignore`。

### 当前验证结果

本机已经成功识别：

```text
供应商：iCode
模型：opus[1m]
来源：CC Switch
```

为避免消耗用户额度，验证过程没有发送真实 AI 对话请求。

## 4. 新增依赖

```text
d3@7.9.0
world-atlas@2.0.2
topojson-client@3.1.0
marked@15.0.12
dompurify@3.4.13
```

依赖版本已写入 `package.json` 和 `package-lock.json`。

## 5. 验证结果

- Python 语法检查通过
- JavaScript 语法检查通过
- 地图和全部本地 vendor 资源返回 HTTP 200
- 轨道 API 成功生成 10 颗卫星、5 个地面站和 60 个时间槽
- 浏览器内实际地图渲染正常
- 播放后时间槽从 1/60 更新到 3/60
- 默认文献目录读取正常
- Markdown 测试文献成功解析为 HTML 标题和正文
- CC Switch 元数据导入成功
- SpaceFL AI 配置文件不包含 Token

## 6. 主要文件

```text
web/orbit_2d_viewer.html
web/server.py
web/index.html
web/assets/app.js
web/assets/app.css
package.json
package-lock.json
.gitignore
2D轨道和文献管理完成报告.md
```

## 7. 未执行事项

- 没有自动提交 Git commit
- 没有自动推送 GitHub
- 没有发送真实 CC Switch AI 请求
